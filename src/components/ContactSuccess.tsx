import React, { FC, useEffect, useState } from 'react';

interface IContactSuccess {
    message: string;
}

const ContactSuccess: FC<IContactSuccess> = () => {
    return (
        <div>test</div>
    );
}

export default ContactSuccess;
