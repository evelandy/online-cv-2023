export default interface IPage {
  name: string;
  globalTheme: string;
}

export default interface IContact {
  name: string;
  company: string;
  email: string;
  message: string;
  document: any;
}
